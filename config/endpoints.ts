// Los endpoints son como etiquetas o rutas que representan recursos en tu app

import { babelIncludeRegexes } from "next/dist/build/webpack-config";

//Endpoints DataBase
export const endpoints = {
  users: "users",
  babies: "babies",
  houses: "houses",
};
